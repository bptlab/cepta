declare module "vue-nprogress";
/*
{
  // export function getRandomNumber(): number

  interface ComponentOptions<V extends Vue> {
    myOption?: string
  }
  new (options: Object): self;

  import {VueConstructor} from "vue";

  interface NProgressOptions {
    minimum: number;
    template: string;
    easing: string;
    speed: number;
    trickle: boolean;
    trickleSpeed: number;
    showSpinner: boolean;
    parent: string;
    positionUsing: string;
    barSelector: string;
    spinnerSelector: string;
  }

  interface NProgress {
    version: string;
    settings: NProgressOptions;
    status: number | null;

    configure(options: Partial<NProgressOptions>): NProgress;
    set(number: number): NProgress;
    isStarted(): boolean;
    start(): NProgress;
    done(force?: boolean): NProgress;
    inc(amount?: number): NProgress;
    trickle(): NProgress;

    render(fromStart?: boolean): HTMLDivElement;
    remove(): void;
    isRendered(): boolean;
    getPositioningCSS(): 'translate3d' | 'translate' | 'margin';
  }

  const nProgress: NProgress;
  export nProgress;

  export interface VueDateTimeConstructor extends VueConstructor {
    showSpinner: boolean; latencyThreshold: number; router: boolean; http: boolean; parent: string;
  }
}
*/
