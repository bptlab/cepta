apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cepta.fullname" . }}-test-connection"
  labels:
    {{- include "cepta.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cepta.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
